▶️ Ejecución
bash# Ejecutar en modo debug
cargo run

# Ejecutar en modo release (recomendado para mejor rendimiento)
cargo run --release

🎮 Controles
AcciónControlRotar cámaraClick izquierdo + arrastrar mouseSalirESC
